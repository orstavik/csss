**description:**
A BoxItem with start and end alignment.
**csss:**
```csss
$boxItem(snapStartEnd,scrollMargin(10px,20px))
```
**css:**
```css
.\$boxItem\(snapStartEnd\,scrollMargin\(10px\,20px\)\) {
  scroll-snap-align: start end;
  scroll-margin-block: 10px;
  scroll-margin-inline: 20px;
}
```
**description:**
A BoxItem with 40px margin.
**csss:**
```csss
$boxItem(scrollMargin(20px*2))
```
**css:**
```css
.\$boxItem\(scrollMargin\(20px\*2\)\) {
  scroll-margin: 40px;
}
```
