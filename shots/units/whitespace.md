**description:**
An example demonstrating prevents text from wrapping to the next line.
**csss:**
```csss
$nowrap
```
**css:**
```css
.\$nowrap {
  white-space: nowrap;
}
```
**description:**
An example demonstrating preserves whitespace and allows wrapping at line breaks.
**csss:**
```csss
$preWrap
```
**css:**
```css
.\$preWrap {
  white-space: pre-wrap;
}
```
**description:**
An example demonstrating collapses whitespace but preserves line breaks.
**csss:**
```csss
$preLine
```
**css:**
```css
.\$preLine {
  white-space: pre-line;
}
```
**description:**
An example demonstrating preserves all whitespace and line breaks exactly as authored.
**csss:**
```csss
$pre
```
**css:**
```css
.\$pre {
  white-space: pre;
}
```
**description:**
An example demonstrating preserves whitespace sequences and breaks at spaces when needed.
**csss:**
```csss
$breakSpaces
```
**css:**
```css
.\$breakSpaces {
  white-space: break-spaces;
}
```
**description:**
An example demonstrating resets whitespace handling to default browser behavior.
**csss:**
```csss
$normal
```
**css:**
```css
.\$normal {
  white-space: normal;
}
```
