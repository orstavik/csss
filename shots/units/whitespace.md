**csss:** $nowrap
**css:**
```css
@layer containerDefault {
  .\$nowrap {
    white-space: nowrap;
  }
}
```

**csss:** $preWrap
**css:**
```css
@layer containerDefault {
  .\$preWrap {
    white-space: pre-wrap;
  }
}
```

**csss:** $preLine
**css:**
```css
@layer containerDefault {
  .\$preLine {
    white-space: pre-line;
  }
}
```

**csss:** $pre
**css:**
```css
@layer containerDefault {
  .\$pre {
    white-space: pre;
  }
}
```

**csss:** $breakSpaces
**css:**
```css
@layer containerDefault {
  .\$breakSpaces {
    white-space: break-spaces;
  }
}
```

**csss:** $normal
**css:**
```css
@layer containerDefault {
  .\$normal {
    white-space: normal;
  }
}
```